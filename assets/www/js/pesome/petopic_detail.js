$('#petopic_detail').live('pageshow', function(event) {    var id = $urlUtility.getVars()["id"];/** *  Get Detail Petopic */    $.ajax({        url: $api_url.petopic_detail(id),        dataType: "jsonp",        contentType: "application/json",        async: false,        success: function (data) {            var petopic = data.petopic;            $('#petopicPic').attr('src', $full_base_url + petopic.thumbnail);            $('#petopicTitle').text(petopic.title);            $('#petopicDesc').text(petopic.description);        },        error: function(e) {            console.log(e.message);        }    });/** *  Get Petopic's peticks */    $.ajax({        url: $api_url.petopic_getpeticks(id),        dataType: "jsonp",        contentType: "application/json",        async: false,        success: function (data) {            $.each(data, function(i, res) {                var petick = res.petick;                var type = petick.tick_attach_type;                switch(type){                    case 'Link':                                                $petick.link(petick);                        break;                    case 'Audio':                        $petick.audio(petick);                        break;                    case 'Video':                        $petick.video(petick);                        break;                    case 'ShareFile':                        //d(petick);//chua co                        break;                    case 'PhotoAlbum':                        //d(petick);                        $petick.photo(petick);                        break;                    default:                        $petick.text(petick.content);                        break;                }            });            $('#petickList').listview('refresh');        },        error: function(e) {            console.log(e.message);        }    });});var $petick = {    link : function(petick){        content = petick.content;        id = petick.id;        link_id = petick.tick_attach_id;        //d(petick);        $('#petickList').append('<li>' +'<a href="petick.html?id='+id+'"><p>' + content + '</p></a></li>');            },    text : function(content){        $('#petickList').append('<li>' +'<a href="#"><p>' + content + '</p></a></li>');    },    photo : function(petick){        album_id = petick.tick_attach_id;        album_name = petick.name;        album_thumb = petick.thumbnail;        $('#petickList').append('<li>' +'<a href="#"><img alt="'+album_name+'" src="' + $full_base_url + album_thumb  + '"/><p>' + album_name + '</p></a></li>');    },    video : function(petick){        video_id = petick.tick_attach_id;        $.ajax({            url: $api_url.petopic_getvideo(video_id),            dataType: "jsonp",            contentType: "application/json",            async: false,            success: function (data) {                              $('#petickList').append('<li>' +'<p>' + '<video poster="' + $full_base_url + data.video.thumb  + '" controls="controls"><source src="' + $full_base_url + data.video.video  + '">Your browser does not support the audio element.</video></p></li>');               $('#petickList').listview('refresh');            },            error: function(e) {                console.log(e.message);            }        });    },    audio : function(petick){        id = petick.id;        content = '>>AUDIO<<'; //need change to audio image         $('#petickList').append('<li>' +'<a href="petick_audio.html?id='+id+'"><p>' + content + '</p></a></li>');            }};